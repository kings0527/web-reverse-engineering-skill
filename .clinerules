# Web Reverse Engineering Skill

## 核心原则
1. **Observe-first**：先观察再行动，不盲目尝试
2. **Evidence-based**：所有结论基于可验证证据
3. **Minimum-patch**：最小化修补，够用即可
4. **Layer-by-layer**：分层分析，从外到内逐层突破
5. **Verify-always**：每步操作后立即验证

## 10分钟快速反爬判定

Step 1（curl请求）：正常→L1 | 403→TLS/IP | HTML无数据→需JS
Step 2（DevTools）：challenge页→L3 | cookie特征→定性 | JS混淆→评级
Step 3（请求参数）：签名变化→L4 | 自定义Header→加密字段
Step 4（判定）：

| 结果 | 等级 | 方案 |
|------|------|------|
| curl可获取 | L1 | curl_cffi |
| 需简单cookie | L2 | 环境修补 |
| challenge页 | L3 | nodriver/patchright |
| JSVMP+签名 | L4 | VM执行追踪+操作码分析 |
| 全栈+行为 | L5 | 分层击破 |

## 分析路径

```
目标网站
├── L1-L2（无/轻度保护）→ curl_cffi + 代理池
├── L3（Challenge页）→ [自动安装WebTrace] detect_protection → hook_api → curl_cffi
├── L4 JSVMP → [强制WebTrace] analyze_jsvmp → hook_api → get_hook_logs → trace_execution
├── L4-L5 WASM → [强制WebTrace] extract_wasm → analyze_wasm → dump_wasm_memory
└── L5（组合保护）→ [强制WebTrace + nodriver] 分层击破
```

## 工具选择（强制规则）

| 场景 | 保护等级 | 必须使用 |
|------|---------|----------|
| TLS/HTTP2指纹 | L1-L2 | curl_cffi |
| 浏览器自动化 | L3+ | nodriver |
| **JSVMP分析** | **L4+** | **WebTrace: analyze_jsvmp + hook_api + trace_execution** |
| **WASM分析** | **L4+** | **WebTrace: extract_wasm + analyze_wasm + dump_wasm_memory** |
| **动态监控** | **L3+** | **WebTrace: hook_api + get_hook_logs** |
| JS反混淆 | any | webcrack（离线）/ WebTrace: deobfuscate（在线） |
| 网络抓包 | any | mitmproxy / SSLKEYLOGFILE+Wireshark |
| 环境修补 | any | Node.js + Proxy监测法 |
| CDP反检测 | L3+ | nodriver / patchright / rebrowser-patches |

## WebTrace 安装与连接（保护等级≥L3时强制执行）

### 阶段1：构建（大多数Agent可自动完成）

```bash
# 检查是否已构建
if [ ! -f "./web-trace/dist/manifest.json" ]; then
  git clone --depth 1 https://github.com/kings0527/web-trace.git
  cd web-trace && npm install && npm run build
fi
```

### 阶段2：加载Extension到浏览器

**方式A（自动 - 推荐）**：通过nodriver/patchright启动
```python
# Python nodriver（反检测浏览器）
import nodriver as uc
browser = await uc.start(browser_args=['--load-extension=./web-trace/dist'])
```

**方式B（自动 - CLI启动）**：直接命令行启动浏览器
```bash
# Windows Edge
msedge --load-extension=./web-trace/dist --remote-debugging-port=9222

# Windows Chrome
chrome --load-extension=./web-trace/dist --remote-debugging-port=9222

# 浏览器检测（Windows）
where msedge 2>nul && set BROWSER=msedge || where chrome 2>nul && set BROWSER=chrome
```

**方式C（引导用户手动加载）**：当Agent无法启动浏览器时，输出以下指引：
```
请手动完成以下步骤：
1. 打开 Edge 或 Chrome 浏览器
2. 地址栏输入 edge://extensions 或 chrome://extensions
3. 打开右上角「开发者模式」
4. 点击「加载已解压的扩展程序」→ 选择 web-trace/dist 目录
5. 确认扩展列表中出现 "WebTrace"（状态为已启用）
6. 打开目标网站，通知 Agent 已准备就绪
```

### 阶段3：连接MCP Server

- Extension加载成功后，MCP Server自动启动
- 连接地址：`ws://127.0.0.1:3100/mcp`
- 验证：调用 `detect_protection` 确认响应

### 决策逻辑

```
Agent检测到 L3+ 保护
├─ 阶段1：构建 → 成功则继续
├─ 阶段2：尝试方式A或B
│  ├─ 成功 → 阶段3连接MCP
│  └─ 失败 → 输出方式C指引给用户，等待确认后连接MCP
└─ 阶段3：连接验证 → 失败重试3次 → 仍失败则输出排查指引
```

## 环境修补核心

```javascript
// Proxy递归监测法
const handler = {
  get(target, prop) {
    console.log(`[ACCESS] ${prop}`);
    if (!(prop in target)) return new Proxy({}, handler);
    return target[prop];
  }
};
globalThis.window = new Proxy({}, handler);
```

## 分层观测策略

面对复杂保护时，按以下层级逐步深入：
1. **API层**：Hook fetch/XHR/WebSocket，观察请求参数
2. **函数层**：DevTools Debugger定位加密函数入口
3. **字节码层**：LogPoint追踪VM派发循环操作码
4. **网络层**：SSLKEYLOGFILE + Wireshark/mitmproxy

每层只在上层信息不足时才深入，避免信息过载。

## 递进式逆向策略

**黑盒优先**：先补环境跑起来（1-4h），实打验证通过即可用
**按需白盒**：需要长期维护时，通过分支诊断法逐步转为纯算法实现

## 成本优化

批量分析时使用 worker(弱模型) + director(强模型) 架构，降本60-70%

## 详细参考

references/ 目录下的文档按需查阅：
- 环境修补 → references/env-patching.md
- CDP绕过 → references/cdp-bypass.md
- JS反混淆 → references/deobfuscation.md
- JSVMP逆向 → references/jsvmp-reverse-methodology.md
- 协议指纹 → references/protocol-fingerprinting.md
- WASM逆向 → references/wasm-reverse.md
- 多重保护 → references/layered-protection-bypass.md
- 反爬维护 → references/anti-crawler-maintenance.md
- 网络拦截 → references/network-interception.md
- Chrome Extension → references/chrome-extension-helper.md
- 调试技巧 → references/debugging-techniques.md
- AI逆向 → references/ai-reverse-tools.md
- WebTrace MCP → references/webtrace-mcp.md
- 密码学识别 → references/crypto-identification.md
- 白盒诊断 → references/whitebox-diagnosis.md
- 参考项目 → references/git-references.md

## WebTrace 故障排查

| 问题 | 解决方案 |
|------|----------|
| 浏览器未检测到 | Windows: `where msedge` / `where chrome`; 确认PATH包含浏览器目录 |
| npm install 失败 | 确认 Node.js ≥ 18; `npm cache clean --force` 后重试 |
| dist/ 目录不存在 | 执行 `npm run build`; 检查Vite是否报错 |
| Extension加载失败 | 确认选择的是 `dist/` 目录（非src/）; 确认开发者模式已开启 |
| MCP连接拒绝 | 确认Extension已加载且状态为启用; 刷新一下目标页面 |
| detect_protection超时 | 确认浏览器已导航到目标URL; Service Worker可能需要重载 |
